# Context Passport Conformance Tests

The conformance test suite has moved to its own repository:

**[github.com/contextpassport/conformance-tests](https://github.com/contextpassport/conformance-tests)**

That repository contains the canonical vectors, the polyglot harness (Python ↔ TypeScript byte-equivalence), and CI integration examples. As of v2.0, the harness passes 11/11 cross-implementation vectors including non-ASCII and emoji payloads.

This stub remains for backward-compatible link resolution. Open issues, PRs, and discussions about conformance against the repo above.
